import Layout from '@/layout'
export default {
  path: '/{{ name }}',
  component: Layout,
  children: [{
    // 默认路径,访问父路由/{{ name }}直接方法这个子路由
    path: '',
    name: '{{ name }}',
    component: () => import('@/views/{{ name }}/index.vue'),
    meta: { title: '{{ title }}', icon: '' }
  }]
}
